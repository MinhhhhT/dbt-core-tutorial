jaffle_shop: # this needs to match the profile in your dbt_project.yml file
    target: dev
    outputs:
        dev:
            type: postgres
            host: localhost
            user: postgres
            password: "{{ env_var('DBT_PASS') }}"
            port: 5432
            dbname: jaffle_shop
            schema: analytics
            threads: 4